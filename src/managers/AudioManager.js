class AudioManager {
    constructor() {
        this.enabled = true;
    }

    playMusic(key) {
        // Hook futuro
    }

    stopMusic() {
        // Hook futuro
    }

    playSFX(key) {
        // Hook futuro
    }
}

export const audioManager = new AudioManager();